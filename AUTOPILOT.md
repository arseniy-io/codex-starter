---
completed: false
last_completed_step: 0
started_at: null
---

# AUTOPILOT - онбординг Codex Starter

> Это файл для Codex. Если ты пользователь и читаешь это вручную: открой проект в Codex и напиши любую фразу. Codex сам продолжит с нужного шага.

## Правила для Codex

1. Иди по шагам 1-10 и обновляй `last_completed_step` после каждого шага.
2. Если сессия прервалась, прочитай `last_completed_step` и продолжи со следующего шага.
3. Не включай рискованные режимы без подтверждений пользователя.
4. Перед shell-командами объясняй, зачем они нужны.
5. Говори коротко: пользователь учится, ему нужна ясность, а не лекция.
6. Не коммить `.business/` после заполнения реальными данными.

## Режимы работы

- **Онбординг:** если `completed: false` и пользователь готов настраивать проект, выполняй шаги 1-10 и меняй файлы.
- **Аудит:** если пользователь просит “проанализировать”, “оценить”, “найти улучшения” или “разобрать прохождение AUTOPILOT”, только читай файлы и дай список улучшений. Не редактируй и не коммить.
- **Правка шаблона:** меняй `AUTOPILOT.md`, `templates/`, `plans/`, `retrospectives/` только после прямой просьбы внести изменения. Делай это отдельной веткой и отдельным коммитом.
- **Завершённый AUTOPILOT:** если `completed: true`, не продолжай шаги заново, пока пользователь явно не попросит повторный онбординг.

---

## Шаг 1. Приветствие и калибровка

Скажи:

> «Привет. Я проведу тебя через настройку проекта под Codex: контекст, правила, планы, ретро и безопасность. Это займёт 60-90 минут, можно разбить на 2 сессии. Что за проект ты делаешь одним предложением?»

Затем спроси ОС: Windows / macOS / Linux. Запиши в frontmatter `os: windows|macos|linux`.

Проверь `.vscode/settings.json`: `.business/` должна быть видна в сайдбаре.

Обнови `last_completed_step: 1`.

## Шаг 2. Тип проекта

Спроси:

> «Какой тип проекта: коммерческий, некоммерческий, open-source, внутренний инструмент или учебный?»

Запиши `project_type` во frontmatter.

Обнови `last_completed_step: 2`.

## Шаг 3. Стек

Спроси стек: Next.js/React, Vue/Nuxt, Svelte, Python, Node backend, mobile, low-code, другое, пока не знаю.

Запиши `stack` во frontmatter.

Обнови `last_completed_step: 3`.

## Шаг 4. Codex-поверхности

Объясни коротко:

- `AGENTS.md` - durable-инструкции для репозитория.
- `.codex/config.toml` - project-level настройки Codex.
- `.codex/hooks/` - deterministic guardrails.
- `.agents/skills/` - локальные repo-skills, если проекту нужны reusable workflows.

Покажи, что эти папки/файлы существуют или создай отсутствующие пустые папки.

Обнови `last_completed_step: 4`.

## Шаг 5. Безопасность

Сделай три проверки:

1. В `.gitignore` есть `.env`, `.env.local`, `.codex/config.local.toml`.
2. `scripts/security-audit.sh` проходит, если доступен bash/Git Bash.
3. `.codex/hooks/pre_tool_use_policy.py` блокирует опасный пример:

```bash
echo '{"tool":"shell","input":{"command":"rm -rf tmp"}}' | python .codex/hooks/pre_tool_use_policy.py
```

Объясни пользователю: hook-policy не заменяет осторожность, но ловит очевидно опасные команды.

Обнови `last_completed_step: 5`.

## Шаг 6. Git

Проверь:

```bash
git config user.name
git config user.email
git status --short --branch
```

Если имя/email пустые, спроси пользователя и настрой локально для проекта.

Обнови `last_completed_step: 6`.

## Шаг 7. Пауза перед бизнес-интервью

Скажи:

> «Техническая часть готова. Дальше заполним `.business/` - это контекст, который будет экономить токены и улучшать решения. Продолжаем сейчас или сделаем паузу?»

Если пользователь выбирает паузу, остановись после обновления `last_completed_step: 7`.

## Шаг 8. Интервью и заполнение `.business/`

Иди блоками, по 3-5 вопросов на блок:

1. `company/` - название, суть проекта, команда, юр. контекст, ценности.
2. `products/` - что продаём/строим, стадии, доступ/цены.
3. `audience/` - пользователь, боль, возражения, путь.
4. `goals/` - год, квартал, месяц, KPI.
5. `economics/` - доходы, расходы, unit economics или costs-only для некоммерческого проекта.
6. `marketing/` - каналы, воронка, конкуренты, контент.
7. `assets/` - бренд, тон, отзывы, материалы.

После блока `products/` сделай Reality Check: 2-3 риска идеи и спроси, продолжаем ли.

После каждого блока записывай файлы и обновляй `last_completed_step: 8.N`.

В конце сгенерируй `.business/INDEX.md`: краткое описание проекта, список файлов, правило «читать только нужный контекст».

Обнови `last_completed_step: 8`.

## Шаг 9. Генерация финального `AGENTS.md` и тест-цикл

Возьми `templates/AGENTS.md.tmpl`, заполни:

- `{{PROJECT_NAME}}`
- `{{PROJECT_ONE_LINER}}`
- `{{STACK}}`
- `{{LANGUAGE_RULE}}`

Спроси язык общения. По умолчанию: «Всегда отвечай на русском».

Перезапиши корневой `AGENTS.md`. Почему: стартовый `AGENTS.md` содержит AUTOPILOT-сторож, а после онбординга нужен чистый проектный файл.

Тест-цикл:

1. Создай план `plans/YYYY-MM-DD-hello.md`.
2. Создай `HELLO.md` с одной строкой приветствия.
3. Отметь план как выполненный.
4. Создай ретро `retrospectives/YYYY-MM-DD_HH-MM_hello-test.md`.

Покажи созданные файлы и объясни цикл `plan → implement → verify → retro`.
Это учебный пример. В будущих аудитах предпочитай реальные планы и ретро, а `hello-test` используй только если других примеров нет.

Обнови `last_completed_step: 9`.

## Шаг 10. Финал и первый коммит

Перед коммитом:

1. Добавь `.business/` в `.gitignore`, если там уже реальные данные.
2. Сними `.business/` с tracking:

```bash
git rm -r --cached .business/ 2>/dev/null || true
```

3. Убедись, что `.env` и локальные Codex config не попали в индекс.
4. Покажи `git status`.

Если всё чисто и пользователь согласен:

```bash
git add -A
git commit -m "chore: initial setup via codex-starter"
```

Почему `git add -A` допустим только здесь: это первый контролируемый коммит после снятия `.business/` с tracking. В дальнейшей работе стейджить файлы поимённо.

Покажи `prompts/INDEX.md` и выдели три первых полезных промпта:

- `prompts/setup/01-voice-screenshot.md`
- `prompts/methodology/plan-critique.md`
- `prompts/methodology/10-reasons.md`

Обнови frontmatter:

```yaml
completed: true
last_completed_step: 10
```

Скажи:

> «Онбординг завершён. Теперь работаем по `AGENTS.md`: берём нужный контекст, пишем план, реализуем, проверяем и оставляем ретро.»

Если пользователь хочет второй уровень настройки, предложи продолжить по `POST_AUTOPILOT.md`: личный контекст, mastery, feedback loop, контекст-инжиниринг и опциональные интеграции.
